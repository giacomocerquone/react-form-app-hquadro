import React from "react";
import Button from "../atoms/Button";

const NavBar = () => {
  return (
    <div style={{ display: "flex" }}>
      <Button />
      <Button />
      <Button />
    </div>
  );
};

export default NavBar;
